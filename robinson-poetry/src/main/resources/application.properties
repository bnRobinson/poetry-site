spring.datasource.url=jdbc:mysql://localhost:3306/writing_robin
spring.datasource.username=writing_robin
spring.datasource.password=writing_robin123

# TODO: learn how to encode username and password for website launch

# Specify the DBMS
spring.jpa.database=MYSQL

#Show or not log for each sql query
spring.jpa.show-sql=true

#hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Use spring.jpa.properties. * for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQL8Dialect
